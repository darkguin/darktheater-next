export enum Route {
  Home = '/',
  SignIn = '/sign-in',
  SignUp = '/sign-up',
  ResetPassword = '/reset-password',
  Confirmation = '/confirm',
  Catalog = '/catalog',
  Profile = '/profile',
}

export const RoutesList = Object.values(Route);